export enum UserRole {
    BUSINESS = 'business',
    USER = 'user',
    ADMIN = 'admin',
}

export enum AuthType {
    OAUTH = 'oauth',
    STD_AUTH = 'std_auth',
}

export enum AuthProvider {
    GOOGLE = 'google',
    FACEBOOK = 'facebook',
    LOCAL = 'local',
    SMS = 'sms',
}
